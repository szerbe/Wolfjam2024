using UnityEngine;

public class level1Init
{
    public level1Init(){
        //char[][] layout = [[]];
        //int[][2] key = [[]];
        int[1][2] character = [0,0];
        //Setup lvl1 = new Setup(layout, key, character);
        //input 2d char array
    }
}
